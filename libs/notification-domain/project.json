{
  "name": "notification-domain",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/notification-domain/src",
  "projectType": "library",
  "tags": ["scope:domain"],
  "// targets": "to see all targets run: nx show project notification-domain --web",
  "targets": {
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "libs/notification-domain/jest.config.ts",
        "passWithNoTests": true
      }
    }
  }
}
