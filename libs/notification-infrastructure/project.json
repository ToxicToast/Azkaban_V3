{
    "name": "notification-infrastructure",
    "$schema": "../../node_modules/nx/schemas/project-schema.json",
    "sourceRoot": "libs/notification-infrastructure/src",
    "projectType": "library",
    "tags": ["scope:infrastructure"],
    "// targets": "to see all targets run: nx show project notification-infrastructure --web",
    "targets": {
        "test": {
            "executor": "@nx/jest:jest",
            "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
            "options": {
                "jestConfig": "libs/notification-infrastructure/jest.config.ts",
                "passWithNoTests": true
            }
        }
    }
}
