{
	"name": "twitch-domain",
	"$schema": "../../node_modules/nx/schemas/project-schema.json",
	"sourceRoot": "libs/twitch-domain/src",
	"projectType": "library",
	"tags": ["scope:domain"],
	"// targets": "to see all targets run: nx show project twitch-domain --web",
	"targets": {
		"test": {
			"executor": "@nx/jest:jest",
			"outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
			"options": {
				"jestConfig": "libs/twitch-domain/jest.config.ts",
				"passWithNoTests": true
			}
		}
	}
}
