{
    "name": "foodfolio-domain",
    "$schema": "../../node_modules/nx/schemas/project-schema.json",
    "sourceRoot": "libs/foodfolio-domain/src",
    "projectType": "library",
    "tags": ["scope:domain"],
    "// targets": "to see all targets run: nx show project foodfolio-domain --web",
    "targets": {
        "test": {
            "executor": "@nx/jest:jest",
            "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
            "options": {
                "jestConfig": "libs/foodfolio-domain/jest.config.ts",
                "passWithNoTests": true
            }
        }
    }
}
